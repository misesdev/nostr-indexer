export type Settings = {
    current_page: number;
    items_per_page: number;
    max_fetch_events: number;
    max_pubkeys: number;
    initial_user: string;
    relay_page: number;
    relays_per_page: number;
    relays: string[];
}
